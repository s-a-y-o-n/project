import 'package:flutter/material.dart';

class HiveHome extends StatelessWidget {
  const HiveHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Welcom onboard'),
      ),
    );
  }
}
